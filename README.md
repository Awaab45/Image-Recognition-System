# Image-Recognition-System
Develop an image recognition system that can classify objects in images. Use a pre-trained deep learning model (such as ResNet or MobileNet) and implement the system using a framework like TensorFlow or PyTorch. Allow users to upload images and get predictions on the content.
